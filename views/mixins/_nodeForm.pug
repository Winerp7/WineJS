mixin nodeForm(node = {})
  form(action=`/add-node/${node._id || ''}` method="POST" class="card")
    div(class="flex flex-col items-center")
      div(class="flex flex-col border rounded-lg bg-white max-w-md")
        div(class="text-gray-600 font-semibold text-lg mx-4 my-5 self-center")
          | Add Node
        div(class="flex flex-row mb-4 mr-4")
          div(class="text-gray-600 ml-4 w-40")
            | Node Identifier
          div(class="text-gray-600")
            input(type="text" name="nodeID", value=node.nodeID, class="border-4 rounded-lg border-gray-200 bg-gray-200 focus:bg-white focus:outline-none")
        div(class="flex flex-row mb-4")
          div(class="text-gray-600 ml-4 w-40")
            | Name
          div(class="text-gray-600")
            input(type="text" name="name" value=node.name, class="w-full border-4 rounded-lg border-gray-200 bg-gray-200 focus:bg-white focus:outline-none")
        div(class="text-gray-600 ml-4 mb-5 self-center")
          | Select sensors
        div(class="flex flex-row mb-4")
          span(class="text-gray-600 ml-4 w-40")
            | Temperature
          div(class="")
            input(type="checkbox" id=sensor value=sensor name="sensors" class="form-checkbox h-4 w-4")
        div(class="flex flex-row mb-4")
          span(class="text-gray-600 ml-4 w-40")
            | Humid
          div(class="")
            input(type="checkbox" id=sensor value=sensor name="sensors" class="form-checkbox h-4 w-4")          
        div(class="flex flex-row mb-4")
          span(class="text-gray-600 ml-4 w-40")
            | Light
          div(class="")
            input(type="checkbox" id=sensor value=sensor name="sensors" class="form-checkbox h-4 w-4")
        div(class="text-gray-600 ml-4 mb-5 self-center")
          button(type="submit" class="text-sm place-self-center px-5 py-1 bg-teal-300 border border-teal-300 rounded-lg text-gray-100 hover:border-teal-300 hover:bg-gray-100 hover:text-teal-300 focus:outline-none") 
            | Add node