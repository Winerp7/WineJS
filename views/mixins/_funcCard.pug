mixin funcCard(func = {})
  form(action=`/functionality/${func._id}/edit` method="GET" class="flex flex-col mx-12 rounded-3xl text-xs my-4 py-6 bg-gray-100 shadow-md")
    div(class="place-self-center text-lg mb-4") 
      | #{func.name}
    div(class="flex flex-row")
      div(class="w-64")
      div(class="w-32")
    div(class="flex flex-col items-center justify-center mb-4")
      div(class="flex flex-col items-center mb-2 w-64 h-48")
        p(class="text-gray-700") Setup()
        p(class="text-xs overflow-auto") #{func.setup}
      div(class="flex flex-col items-center mb-2 w-64 h-48")
        p(class="text-gray-700") Loop()
        div(class="text-xs overflow-auto") #{func.loop}
    div(class="flex flex-row items-center justify-center")
      button(type="submit" class="mt-2 text-sm place-self-center px-5 py-1 bg-teal-300 border border-teal-300 rounded-lg text-gray-100 hover:border-teal-300 hover:bg-gray-100 hover:text-teal-300 focus:outline-none") 
        | Edit functionality
      // TODO: add routing for edit func