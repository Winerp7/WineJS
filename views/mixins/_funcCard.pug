mixin funcCard(func = {})
  form(action=`/functionality/${func._id}/edit` method="GET" class="flex flex-col mx-12 rounded-3xl text-xs my-4 py-6 bg-gray-100 shadow-md max-w-sm")
    div(class="place-self-center text-lg mb-4 border-b") 
      | #{func.name}
    div(class="flex flex-row shrink-1")
      div(class="w-64")
      div(class="w-64")
    div(class="flex flex-col items-center justify-center mb-4 mx-8")
      div(class="flex flex-col items-center mb-3 h-32 w-full")
        div(class="text-gray-700 mb-1") Setup()
        textarea(readonly class="text-xs overflow-auto border border-gray-700 rounded-lg p-2 bg-gray-200 w-full h-full focus:outline-none") 
          | #{func.setup}
      div(class="flex flex-col items-center mb-2 h-32 w-full")
        div(class="text-gray-700 mb-1") Loop()
        textarea(readonly class="text-xs overflow-auto border border-gray-700 rounded-lg p-2 bg-gray-200 w-full h-full focus:outline-none") 
          | #{func.loop}
    div(class="flex items-center justify-center")
      button(formaction=`/functionality/delete/${func._id}` formmethod="GET" type="submit" id="deleteFuncBtn" class="mr-2 text-sm place-self-center px-5 py-1 bg-red-500 border border-red-500 rounded-lg text-gray-100 hover:border-red-500 hover:bg-gray-100 hover:text-red-500 focus:outline-none") 
        | Delete
      button(type="submit" class="text-sm place-self-center px-5 py-1 bg-teal-300 border border-teal-300 rounded-lg text-gray-100 hover:border-teal-300 hover:bg-gray-100 hover:text-teal-300 focus:outline-none") 
        | Edit functionality