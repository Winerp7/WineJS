extends layout

block styles
  link(rel="stylesheet", href="/css/forms.css")
  link(rel="stylesheet", href="/css/product.css")

block content
  main
    form.product-form(action="/user/add-device", method="POST")
    .form-control
      label(for="title") ID
      input(type="text", name="title")#title
      label(for="title") Name
      input(type="text", name="title")#title
      button.btn(type="submit") Add Device