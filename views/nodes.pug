extends _layout

include mixins/_nodeCard

block content
  div(class="flex justify-center")
    h2(class="text-3xl text-gray-900 mb-4 place-self-center")=title
  div(class="flex flex-row flex-wrap items-center justify-evenly")
    each node in nodes
      +nodeCard(node)